import React from "react";

const ContentTitle = ({ title }) => {
  return (
    <div className="ContentTitle border-bottom">
      <h1 className="m-0 pb-3">{title}</h1>
    </div>
  );
};

export default ContentTitle;
